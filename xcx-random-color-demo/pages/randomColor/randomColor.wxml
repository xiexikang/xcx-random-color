<!--pages/randomColor/randomColor.wxml-->

<!-- 自定义颜色标签云 -->
<view class='content'>
  <block wx:for="{{labArr}}" wx:key="key" wx:for-index="key" wx:for-item="v">
    <view class='lab' data-key="{{key}}" style="background-color:{{randomColor[key]}}">{{v}}</view>
  </block>
</view>

